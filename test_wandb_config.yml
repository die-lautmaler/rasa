language: en

pipeline:
- name: WhitespaceTokenizer
- name: CountVectorsFeaturizer
- name: DIETClassifier
  epochs: 3
  learning_rate: 0.001
  batch_size: [32, 64]
  transformer_size: 256
  validation_split: 0.2
  evaluate_every_number_of_epochs: 1
  early_stopping:
    enabled: true
    monitor: "val_t_loss"
    patience: 3
    min_delta: 0.001
assistant_id: 20250827-104256-diagonal-denouement
